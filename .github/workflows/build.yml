name: Build Midpoint Visuals
on: 
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

# Добавляем строгие права на чтение и запись содержимого репозитория
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'microsoft'

      - name: Validate Gradle Wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4 # Обновили до стабильной v4
        with:
          gradle-version: wrapper

      - name: Make Gradle Wrapper Executable
        run: chmod +x gradlew

      - name: Build Mod JAR
        run: ./gradlew build --no-daemon

      - name: Upload Artifact Mod JAR
        uses: actions/upload-artifact@v4
        with:
          name: Midpoint-Visuals-1.21.4
          path: build/libs/*.jar
          
